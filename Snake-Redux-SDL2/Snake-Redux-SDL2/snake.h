#pragma once
#include <iostream>

class Snake{
private:
	int points;
	int current_size;
	bool win_state;
	set_points(int);
	set_current_size(int);

		

public:
	get_points(int);
	get_current_size(int);
	
};
